{
	"name": "@kaged/utils",
	"description": "Shared foundation utilities — fs guards, type guards, formatting, async/stream helpers, Snowflake IDs, fetch-retry, rotating logger, XDG dirs, and more",
	"version": "0.0.1",
	"license": "AGPL-3.0",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/kaged-dev/kaged-utils.git"
	},
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"files": [
		"src"
	],
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"format": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@types/bun": "^1.3.0",
		"typescript": "^5.8.0"
	}
}
